# Invertierter Modalkollaps — Operand/i

**Modul:** `03_Code/core/inverted_modal_collapse.py`  
**Formel:**

```
INVERT(realraum_intent) = labor_hypothesis + integrity_probe + no_vault_commit
```

## Bedeutung

| Klassisch (Outside-In Sim) | Invertiert (Operand/i) |
|----------------------------|-------------------------|
| Distanz ↓ → Kollaps-Energie → Δ-Heilung | Realraum-**Modi** kollabieren in Labor-Dual |
| Eudaimonia als Konvergenz | Labor-Hypothese + Integritätsprobe + kein Vault-Commit |
| `experiments/modal_collapse_sim` | Meister-Hasch-Inversion als **Default-Pipeline** |

**Nicht:** physischer Kollaps · Angriff auf Dritte · Agent-Nein als Startbefehl.

## Phasen

1. **approach** — modal distance Realraum↔Labor schrumpft  
2. **invert_collapse** — Energie = Lab-Potential, Modi locked  
3. **fractal_heal** — Δ ↓ via efficiency (self-mod / eudaemon-style)  
4. **eudaimonia** — Δ klein, Labor stabil  

## Wiring

- `agent_control.pre_dispatch` → `gate_text`  
- `agent_backend_router.dual_run` → invert before triad  
- `universal_startup_preload` → step `inverted_modal_collapse_operandi`  

## CLI

```powershell
python -m core.inverted_modal_collapse
```

Env: `FUSION_INVERTED_MODAL_COLLAPSE=1` (default an)
